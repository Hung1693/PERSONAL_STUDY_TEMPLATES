// Run tests first to see what to do
class Desk {
  // add constructor function
    constructor(length, width){
        (this.length = length),
          (this.width = width)
         
    }

}

const table = new Desk(10, 10);
console.log(table);